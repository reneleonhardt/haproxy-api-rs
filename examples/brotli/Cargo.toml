[package]
name = "haproxy_brotli_filter"
version = "0.0.0"
edition = "2021"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
haproxy-api = { path = "../..", features = ["module"] }
mlua = { version = "0.12", features = ["macros"] }
brotlic = "0.8"
